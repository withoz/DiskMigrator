# 하드디스크 마이그레이션(디스크 전체 클론) 프로그램 — 요구사항 및 개발 계획서

작성일: 2026-07-17

---

## 1. 확정된 방향

| 항목 | 결정 |
|------|------|
| 핵심 기능 | 디스크 **전체 클론** (섹터/블록 단위 복제, OS·부트영역 포함) |
| 대상 OS | **Windows 전용** |
| 언어/런타임 | **C# / .NET 8** |
| GUI | **WPF** (MVVM) |
| 배포 | **GitHub 무료 공개 배포** (오픈소스) |
| 목표 품질 | 배포·상용 수준 (안정성·안전장치·검증 포함) |

이 조합은 "가장 쉽고 GitHub 공개 배포에 좋다"는 판단이 타당합니다. 근거는: (1) WPF는 성숙하고 자료가 많아 학습·개발이 빠릅니다, (2) `.NET 8`은 자체 완결형(self-contained) 게시와 단일 실행 파일 생성을 지원해 사용자가 런타임 설치 없이 exe 하나로 실행 가능합니다, (3) 저수준 디스크 제어에 필요한 Win32 API를 P/Invoke로 직접 호출할 수 있고, VSS·디스크 정보용 성숙한 .NET 라이브러리가 존재합니다.

---

## 2. 정직한 사전 평가 — 이 프로젝트의 진짜 난이도

먼저 솔직하게 말씀드립니다. **"디스크 전체 클론"은 앱 UI가 아니라 데이터 무결성과 부팅 가능성이 핵심인, 실패 시 사용자의 데이터가 통째로 날아갈 수 있는 고위험 유틸리티**입니다. WPF 화면은 전체 작업의 20% 정도이고, 나머지 80%는 눈에 안 보이는 저수준 처리(원시 디스크 I/O, 볼륨 잠금, 스냅샷, 파티션/부트 처리, 오류 복구)입니다.

특히 어려운(그리고 상용 도구들이 실제로 고생하는) 지점은 다음과 같습니다. 이 계획서는 이 문제들을 단계별로 나눠 위험을 통제하는 것을 목표로 합니다.

- **실행 중인 시스템 디스크(C:) 클론**: 지금 부팅되어 사용 중인 디스크를 그대로 읽으면 파일이 계속 바뀌어 일관성이 깨집니다. 상용 도구는 **VSS(볼륨 섀도 복사)** 스냅샷을 떠서 그 시점의 정지된 이미지를 복제합니다.
- **대상 디스크가 원본보다 작을 때**: 순수 섹터 복제로는 **불가능**합니다. 파티션을 먼저 축소하고 사용 중인 블록만 옮기는 파일시스템 인식 처리가 필요합니다.
- **부팅 가능성 유지**: MBR/GPT, BIOS/UEFI, GPT 백업 헤더 위치 보정 등을 정확히 처리해야 클론한 디스크로 실제 부팅됩니다.
- **불량 섹터·읽기 오류**: 재시도/건너뛰기/로깅 정책이 없으면 오래된 HDD에서 중간에 멈춥니다.

→ **권장 전략: "동작하는 최소 제품(MVP)부터 좁게 시작"**. 아래 로드맵은 위험이 낮은 것부터(오프라인·같은 크기 이상 디스크의 전체 섹터 복제) 시작해, 검증이 쌓인 뒤에 위험한 기능(실행 중 시스템 디스크, 크기 축소 클론)을 얹는 순서로 짰습니다.

---

## 3. 기능 요구사항 (Functional Requirements)

### 3.1 필수 (MVP 범위)
- **디스크 목록 조회**: 연결된 물리 디스크를 모델명·용량·시리얼·버스 종류(SATA/NVMe/USB)·파티션 구성과 함께 나열.
- **원본/대상 선택 UI**: 명확한 화살표(원본 → 대상) 표현.
- **섹터/블록 단위 전체 복제 엔진**: 원본 디스크의 모든 섹터를 대상으로 순차 복제(부트영역·파티션 테이블 포함).
- **크기 검증**: 대상 용량 ≥ 원본 용량일 때만 전체 섹터 복제 허용. 미달 시 명확히 거부.
- **진행 표시**: 진행률(%), 복사 속도(MB/s), 경과/남은 시간(ETA), 현재 오프셋.
- **안전장치(가장 중요)**:
  - 대상 디스크가 시스템 디스크이거나 원본과 동일 디스크면 차단.
  - 대상 디스크에 기존 데이터가 있으면 **"모든 데이터가 삭제됩니다"** 명시 + 사용자가 대상 디스크 모델명을 직접 입력/확인해야 진행.
- **작업 로그**: 시작/종료 시각, 복사 바이트, 오류 섹터, 결과를 파일로 기록.

### 3.2 상용 수준 확장 (MVP 이후)
- **검증(Verification)**: 복제 후 원본·대상의 해시(예: 블록 단위 SHA-256/CRC) 비교로 무결성 확인.
- **불량 섹터 처리 정책**: 재시도 횟수, 실패 시 0으로 채우고 계속, 오류 목록 리포트.
- **일시정지/재개, 취소**: 중단 시 안전하게 롤백/중지.
- **스마트 클론(사용 블록만 복사)**: NTFS `$Bitmap`을 읽어 사용 중인 클러스터만 복제 → 속도 대폭 향상 + 대상<원본도 가능.
- **실행 중 시스템 디스크 클론**: VSS 스냅샷 기반 핫 클론.
- **파티션 자동 확장**: 대상이 더 크면 마지막 파티션을 남는 공간까지 확장.
- **디스크 → 이미지 파일(.img/.vhdx) 백업 및 복원**.
- **예약 실행/스케줄, 다국어(한/영)**.

---

## 4. 비기능 요구사항 (Non-Functional Requirements)

- **안전성 최우선**: 대상 디스크에 쓰기 전 반드시 다중 확인. 오탐지로 인한 데이터 삭제를 구조적으로 막는 것이 1순위.
- **성능**: 대용량 버퍼(예: 1~4MB)와 비동기 I/O로 디스크 대역폭에 근접. UI 스레드와 복제 작업 스레드 분리(백그라운드).
- **신뢰성**: 오류·전원차단·중단 상황에서도 데이터가 예측 가능한 상태로 남고, 로그로 추적 가능.
- **권한**: 원시 디스크 접근은 **관리자 권한 필수** → 실행 시 UAC 상승 요청(app.manifest `requireAdministrator`).
- **배포성**: `.NET 8` 자체 완결형 단일 exe로 게시(런타임 미설치 PC에서도 실행). GitHub Releases 배포.
- **투명성/신뢰(오픈소스 배포 특성)**: 코드 공개, 명확한 라이선스, 강력한 면책 고지("데이터 손실 위험, 사용 전 백업 권고").

---

## 5. 핵심 기술 과제 (Windows 저수준)

| 과제 | 방법 |
|------|------|
| **원시 디스크 읽기/쓰기** | `\\.\PhysicalDriveN` 경로를 Win32 `CreateFile`(P/Invoke)로 열고 섹터 정렬된 버퍼로 읽기/쓰기. |
| **관리자 권한** | app.manifest에서 `requestedExecutionLevel = requireAdministrator`. |
| **대상 볼륨 잠금** | Windows는 Vista 이후 마운트된 볼륨의 섹터 쓰기를 차단 → `DeviceIoControl`로 `FSCTL_LOCK_VOLUME` / `FSCTL_DISMOUNT_VOLUME` 후 써야 함. |
| **디스크 지오메트리·크기** | `IOCTL_DISK_GET_DRIVE_GEOMETRY_EX`, `IOCTL_DISK_GET_LENGTH_INFO`. 디스크 메타(모델/시리얼)는 WMI(`Win32_DiskDrive`) 또는 `Windows.Devices`. |
| **실행 중 시스템 디스크** | **AlphaVSS**(MIT 라이선스 .NET VSS 래퍼)로 섀도 복사본 생성 후 그 스냅샷을 소스로 읽기. |
| **파티션/부트** | MBR vs GPT 구분, GPT는 디스크 끝의 **백업 헤더** 위치를 대상 크기에 맞게 보정. UEFI/BIOS 부팅 요소 보존. |
| **대상 < 원본** | 섹터 복제 불가 → 파티션 축소 + 사용 블록만 복제(스마트 클론)로만 가능. MVP에서는 범위 제외. |

---

## 6. 아키텍처 설계

계층을 분리해 위험한 저수준 코드와 UI를 격리합니다.

```
┌─────────────────────────────────────────────┐
│  UI 계층 (WPF, MVVM)                          │
│  - 디스크 선택 화면 / 진행률 / 확인 다이얼로그 │
│  - ViewModel ↔ 엔진은 이벤트·진행 콜백으로 통신 │
└───────────────┬─────────────────────────────┘
                │ (인터페이스, DTO)
┌───────────────▼─────────────────────────────┐
│  코어 엔진 (플랫폼 독립 로직, .NET 라이브러리) │
│  - CloneJob: 복제 오케스트레이션/진행/취소     │
│  - Verifier: 해시 검증                         │
│  - SafetyGuard: 대상 검증 규칙                 │
└───────────────┬─────────────────────────────┘
                │ (추상화: IDiskAccess, ISnapshot)
┌───────────────▼─────────────────────────────┐
│  Windows 저수준 계층 (P/Invoke)               │
│  - RawDisk(CreateFile/Read/Write)             │
│  - VolumeLock(DeviceIoControl)                │
│  - DiskEnumerator(WMI/IOCTL)                  │
│  - VssSnapshot(AlphaVSS)                       │
└─────────────────────────────────────────────┘
```

- **왜 이렇게?** 저수준 계층을 인터페이스 뒤에 두면, 위험한 실제 디스크 쓰기 대신 "가짜 대상(파일)"으로 엔진을 **안전하게 테스트**할 수 있습니다. 상용 수준 신뢰성을 확보하는 핵심입니다.

### 권장 라이브러리
- **CommunityToolkit.Mvvm** — MVVM 보일러플레이트 축소.
- **AlphaVSS** — VSS 스냅샷 (실행 중 디스크 클론 단계에서).
- **DiscUtils** (선택) — 파티션 테이블/파일시스템 파싱(스마트 클론·이미지 파일 단계에서 유용).
- 로깅: **Serilog** 또는 `Microsoft.Extensions.Logging`.
- 테스트: **xUnit**.

---

## 7. 단계별 개발 로드맵

각 단계는 "그 자체로 동작·검증 가능한 결과물"을 목표로 합니다. 위험이 낮은 것부터 쌓습니다.

### Phase 0 — 프로젝트 골격 (0.5주)
- .NET 8 WPF 솔루션 생성, 3계층 프로젝트 분리(UI/Core/Windows), Git 저장소·라이선스·README 초기화.
- app.manifest로 관리자 권한 상승 적용.
- **완료 기준**: 빈 앱이 관리자 권한으로 실행됨.

### Phase 1 — 디스크 조회 & 안전한 선택 UI (1주)
- WMI/IOCTL로 물리 디스크 목록 표시(모델·용량·파티션).
- 원본/대상 선택 UI + SafetyGuard(시스템 디스크·동일 디스크 차단, 대상 확인 절차).
- **아직 복제는 안 함.** 오탐지 없이 디스크를 정확히 식별하는지 검증.

### Phase 2 — 오프라인 전체 섹터 복제 엔진 (핵심 MVP) (2주)
- `RawDisk` 읽기, 대상 볼륨 잠금/디스마운트, 섹터 순차 복제.
- 대용량 버퍼 + 백그라운드 스레드 + 진행률/속도/ETA.
- **먼저 파일→파일, 그다음 USB 등 비시스템 디스크로 실기 테스트.**
- **완료 기준**: 대상 ≥ 원본인 비시스템 디스크를 복제하고, 복제된 디스크가 원본과 동일하게 마운트/읽힘.

### Phase 3 — 검증 & 오류 처리 (1주)
- 블록 단위 해시 비교 검증.
- 불량 섹터 재시도/건너뛰기/로깅, 취소·정리.
- **완료 기준**: 손상 섹터가 있어도 안전하게 완료 또는 명확히 중단 + 리포트.

→ **여기까지가 GitHub 첫 공개(v0.x) 권장 지점.** "오프라인 디스크 전체 클론 + 검증"만으로도 유용하고 위험이 통제됩니다.

### Phase 4 — 실행 중 시스템 디스크 클론 (VSS) (2주)
- AlphaVSS로 스냅샷 생성 → 스냅샷을 소스로 복제.
- 부팅 요소(MBR/GPT, GPT 백업 헤더 보정) 검증.
- **완료 기준**: 실행 중 C: 포함 디스크를 클론해 대상으로 실제 부팅 성공(테스트 PC/가상머신).

### Phase 5 — 스마트 클론 & 파티션 확장 (2~3주)
- NTFS `$Bitmap` 기반 사용 블록만 복제 → 속도↑, 대상<원본 지원.
- 대상이 크면 마지막 파티션 자동 확장.

### Phase 6 — 상용 마감 (지속)
- 디스크→이미지 파일 백업/복원, 다국어, 스케줄.
- 설치 관리자/서명, 문서, 이슈 템플릿, CI(빌드·테스트 자동화).

**개략 일정**: MVP(Phase 0~3)까지 약 4.5~5주(1인 기준, 집중도에 따라 변동). 상용 수준(Phase 4~6)은 추가 6~8주 이상. — 이는 예측치이며 실제 디스크 테스트에서 변수가 큽니다.

---

## 8. 위험·법적·안전 고려사항

- **데이터 손실 책임**: 이 부류 도구는 오작동 시 사용자 디스크를 파괴할 수 있습니다. README·앱 내에 강한 면책 고지와 "사용 전 백업" 경고 필수.
- **오픈소스 라이선스**: 코드 라이선스(MIT/GPL 등) 선택 + 사용 라이브러리 라이선스 호환성 확인(AlphaVSS는 MIT, DiscUtils는 MIT 계열).
- **테스트 안전**: 실제 디스크 쓰기 테스트는 **반드시 버려도 되는 USB/HDD 또는 가상머신**에서. 초기 개발은 파일 기반 가짜 대상으로.
- **보안 인식**: 원시 디스크 접근 도구는 백신이 의심할 수 있음 → 코드 서명 고려, 공개 저장소로 투명성 확보.
- **관리자 권한 오남용 방지**: 대상 확인 로직을 이중·삼중으로.

---

## 9. 지금 바로 시작할 것 (Next Steps)

1. **저장소 준비**: GitHub 저장소 생성, 라이선스(MIT 권장) 결정, README에 프로젝트 목표·경고 문구 초안.
2. **Phase 0 골격 생성**: .NET 8 WPF 3계층 솔루션 + 관리자 매니페스트. — 원하시면 제가 이 스캐폴딩 코드(솔루션 구조 + 관리자 권한 매니페스트 + 디스크 목록 조회 기본 화면)를 바로 만들어 드릴 수 있습니다.
3. **Phase 1 착수**: 디스크 조회 + 안전한 선택 UI부터 구현(복제는 나중).

---

*이 문서의 일정·난이도는 도메인 지식과 검증된 기술 사실에 기반한 예측이며, 실제 디스크 환경 테스트 결과에 따라 조정이 필요합니다.*
