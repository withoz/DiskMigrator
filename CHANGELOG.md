# 변경 기록

DiskMigrator의 각 버전에서 무엇을 왜 고쳤는지 남깁니다.

GitHub 릴리스 페이지에는 최신 버전만 둡니다 — 옛 실행 파일에는 이후에 고친 결함이
그대로 있어 받아 쓰면 안 되기 때문입니다. 다만 그 기록까지 사라질 이유는 없어
여기에 옮겨 둡니다. 각 버전의 코드는 같은 이름의 git 태그로 볼 수 있습니다.


---

## v0.8.3 — 부팅 검사가 완료 화면 버튼을 숨기던 버그 수정

*2026-07-23 · 태그 `v0.8.3`*

완료 화면에서 **`부팅 구성 검사`를 누르면 그 아래 `UEFI로 변환`·`파티션 확장`·`대상 안전하게 제거` 버튼이 사라지던** 문제를 고쳤습니다. 검사 시작 시 상태를 초기화하는 `ResetBootCheck()`가 검사 표시 상태뿐 아니라 후속 작업 버튼의 표시 여부까지 함께 꺼버린 탓이었습니다. 이제 검사·복구는 그 버튼들을 건드리지 않고, 후속 작업 상태 초기화는 새 선택으로 돌아가거나 새 클론을 시작할 때만 일어납니다.

---

## v0.8.2 — 다른 PC 부팅 실증 반영: 최대 절전 자동 무력화 + UEFI 변환 수정

*2026-07-23 · 태그 `v0.8.2`*

실제로 클론을 다른 PC에 꽂아 부팅해 본 결과를 엔진에 반영했습니다.

### 최대 절전(hiberfil) 자동 무력화

원본이 **빠른 시작(기본 켜짐)**이나 최대 절전 상태로 꺼져 있으면, 그 사본을 다른 하드웨어에서 켤 때 winresume가 **원본 하드웨어의 세션을 복원하려다 로고 동그라미에서 멈춥니다.** 이제 하드웨어 독립화 단계에서 대상에 대해 자동으로:

- `hiberfil.sys` 삭제 (SYSTEM 소유라 소유권을 가져와 삭제 → cold boot 강제)
- 빠른 시작(`HiberbootEnabled`)·최대 절전(`HibernateEnabled`)을 SYSTEM 하이브에서 끔 (재생성 방지)

전에는 이걸 손으로 해야 했는데, 이제 클론만 하면 됩니다.

### UEFI 변환이 "OS 파티션을 못 찾음"으로 실패하던 문제

리사이즈로 복구 파티션이 옮겨지면 BCD의 복구 항목이 옛 위치를 가리키고, `mbr2gpt`가 **`Cannot find OS partition(s)`**로 거부합니다. 이 항목을 지우는 코드가 있었지만 **깨진 항목은 텍스트로 못 찾아 남겨두고** 있었습니다. 이제 `recoverysequence`가 가리키는 복구 항목을 **GUID로 직접 삭제**해, device가 해석되지 않는 항목까지 확실히 정리합니다.

또한 변환 실패 시 `mbr2gpt`의 **진짜 오류 줄**을 보여줍니다 — 전에는 무의미한 첫 줄("Attempting to validate disk N")만 나와 원인을 알 수 없었습니다.

---

## v0.8.1 — 복제 후 대상 USB 안전하게 제거

*2026-07-22 · 태그 `v0.8.1`*

복제가 끝난 뒤 이동식(USB) 대상을 "안전하게 제거"하려 하면 Windows가 **"이 장치는 현재 사용 중입니다"**라며 막히던 상황을 앱에서 바로 해결합니다.

원인은 손상이 아니라 정상 동작이었습니다 — 복제가 끝나면 대상 디스크를 다시 온라인으로 올리는데(파티션 확장·하드웨어 독립화가 볼륨에 접근해야 하므로), 그 순간 Windows가 복제된 볼륨을 자동 마운트합니다. 마운트된 볼륨을 쓰는 것이 있으니 Windows가 제거를 막습니다.

완료 화면에 대상이 USB일 때 **`대상 안전하게 제거`** 버튼이 나옵니다. 누르면 볼륨을 디스마운트하고 디스크를 오프라인으로 내려(디스크 관리의 "오프라인"과 같은 동작, 캐시도 비움), 그대로 USB를 뽑아도 안전합니다. 복제된 데이터에는 영향이 없습니다.

---

## v0.8.0 — 상품화: 영어 지원 · 설치 프로그램 · 시작 화면

*2026-07-22 · 태그 `v0.8.0`*

기능은 그대로 두고, 처음 쓰는 사람이 받아서 바로 쓸 수 있는 판으로 다듬었습니다.

### 한국어 · English

앱 전체를 영어로도 씁니다. 직역이 아니라 실제로 쓰는 표현으로 옮겼습니다. 처음에는 Windows 언어를 따라가고, 헤더의 **`한국어 · English`**를 누르면 그 자리에서 바뀌며 다음에 켤 때도 선택이 유지됩니다. 부팅 검사·엔진 메시지·심각도 배지까지 모두 포함합니다.

### 설치 프로그램

`DiskMigrator-Setup-v0.8.0.exe` 하나로 설치합니다. 사용 조건(EULA)에 동의하면 Program Files에 넣고, 시작 메뉴 바로가기와 제거 프로그램을 만듭니다. 런타임이 없는 PC에서도 그대로 실행됩니다(단일 자체 포함 exe).

### 시작 화면 · 새 아이콘

앱을 켜면 DM 로고 시작 화면이 잠깐 뜬 뒤 메인 창이 나옵니다. 실행 파일과 창·문서에 새 DM 아이콘을 적용했습니다.

### 리사이즈 안전장치

- **데이터가 든 만큼보다 작게는 못 줄입니다.** 손잡이가 그 아래로 내려가지 않고, 한계를 빨간 점선으로 보여 줍니다.
- 화면에 반올림해 보이던 크기 때문에 "대상 용량을 넘습니다"라며 잘못 거절하던 문제를 고쳤습니다(1 GiB 여유).
- 대상 디스크를 바꿀 때 남아 있던 값이 새 대상에 맞게 자동으로 보정됩니다.

> ⚠️ 아직 코드 서명 인증서가 없어, 처음 실행하면 SmartScreen 경고가 뜰 수 있습니다. `추가 정보 → 실행`으로 진행하시면 됩니다.

---

## v0.7.0 — MBR 원본 파티션 조정 + UEFI 변환

*2026-07-20 · 태그 `v0.7.0`*

MBR 원본도 파티션을 넓히며 복제할 수 있게 됐고, 그렇게 만든 사본을 요즘 PC에서 부팅되게 바꿀 수 있습니다. 실제 디스크 이관으로 확인한 판입니다 — SATA에 있던 MBR Windows를 NVMe로 옮겨 부팅까지 마쳤습니다.

### MBR 원본도 `파티션 조정`

지금까지 이 방식은 GPT 원본만 됐습니다. 파티션을 옮기면 파티션 테이블도 새 위치를 가리키게 고쳐야 하는데 그럴 수단이 GPT에만 있었기 때문입니다. 이제 MBR도 됩니다.

MBR은 조심할 곳이 많아 다음을 지킵니다:

- **디스크 서명과 부트 코드를 건드리지 않습니다.** Windows는 "디스크 서명 + 파티션 위치"로 부팅 볼륨을 찾습니다. 서명이 바뀌면 사본이 부팅하지 못합니다. 쓰는 범위를 파티션 항목 네 개로만 한정했습니다.
- **확장 파티션(논리 드라이브)이 있으면 시작 전에 거절합니다.** 논리 드라이브는 사슬처럼 이어져 있어 반만 고치면 통째로 잃습니다.
- **약 2 TB를 넘는 대상은 막습니다.** MBR이 그보다 뒤를 가리킬 수 없습니다.
- 옮겨진 파티션의 **부트 섹터에 새 위치를 적습니다.**

### 요즘 PC에서 부팅되게 만들기

BIOS 방식으로 복제된 사본은 **NVMe M.2 슬롯이나 UEFI 전용 PC에서는 부팅하지 않습니다.** NVMe에는 레거시 부팅용 장치 ROM이 사실상 없어서, BIOS가 "부팅 가능한 장치 없음"이라고 합니다.

복제가 끝나면 완료 화면에 **`UEFI로 변환`** 버튼이 나옵니다. GPT로 바꾸고 EFI 시스템 파티션을 만들어 UEFI로 부팅되게 합니다. 되돌릴 수 없으므로 자동으로 하지 않고 직접 누르실 때만 진행합니다.

### 복제해도 부팅이 안 될 조합을 시작 전에 알립니다

전에는 복제가 끝나고 나서야, 때로는 다른 PC에 꽂아 보고 나서야 알 수 있었습니다.

- **원본이 BIOS 방식입니다** — 사본은 레거시 부팅을 지원하는 PC에서만 켜집니다
- **원본에 최대 절전 이미지가 있습니다** — 사본이 검은 화면에서 멈춥니다

### 검은 화면에서 멈추던 문제

가장 찾기 어려웠던 것입니다. 부트로더·BCD·드라이버가 전부 정상인데 사본이 **오류 문구 하나 없이** 검은 화면에서 멈췄습니다.

원인은 원본이 **빠른 시작으로 종료**돼 있었던 것입니다. Windows 기본값입니다. 그러면 커널 상태를 저장해 두고 다음 부팅에서 정상 부팅 대신 그것을 복원하는데, 저장된 상태는 원래 하드웨어를 전제하므로 다른 메인보드에서는 그대로 멈춥니다. **부팅 실패가 아니라 재개 실패**였습니다.

이제 부팅 검사가 이것을 잡아내고, `부팅 복구`가 함께 해결합니다.

### 부팅 검사·복구가 MBR에서도 동작합니다

- 검사가 **MBR 디스크의 참조도 대조**합니다. 예전에는 GPT만 보고 MBR은 건너뛰어, 참조가 어긋난 디스크를 "부팅 준비 완료"라고 답했습니다.
- 복구가 **BIOS 방식 사본도 고칩니다.** 예전에는 EFI 파티션을 찾다가 "찾지 못했습니다"로 끝났습니다.

### 막대에서 끌어 크기 맞추기

`파티션 조정`을 고르면 `변경 후` 막대에 빨간 손잡이가 생깁니다. 끌어서 대략 맞추고, `←` `→` 키로 정밀하게(Shift를 누르면 크게), 정확한 값은 숫자로 넣습니다. 셋이 같은 값을 함께 물고 있습니다.

넓힐 파티션은 드롭다운 대신 칩으로 고릅니다.

### 그 밖에

- **스마트 클론**과 **새 하드웨어 대비**를 기본으로 켭니다. 대부분의 디스크는 실데이터가 용량보다 훨씬 적어 복제가 크게 빨라지고, 다른 PC로 옮기는 것이 이 도구의 주 용도이기 때문입니다.
- `파티션 조정`를 **`파티션 조정`**으로 바꿨습니다.
- 결과 요약이 MBR 디스크에 "GPT:"라고 적던 것을 고쳤습니다.

테스트 **210개** 통과.

---

**설치**: `DiskMigrator-v0.7.0-win-x64.exe`를 받아 관리자 권한으로 실행하십시오. .NET 설치가 필요 없는 단일 실행 파일입니다.


---

## v0.6.2 — 옵션이 실제로 동작하는지 끝까지 대조

*2026-07-20 · 태그 `v0.6.2`*

화면의 모든 옵션을 엔진까지 따라가 대조했습니다. 켜 놓으면 동작한다고 믿게 되지만 실제로는 아무 일도 하지 않던 것들을 잡았습니다.

### 조용히 무시되던 것

- **스마트 클론이 버려지고 있었습니다.** `VSS 스냅샷 사용`을 끄면 스마트 클론 체크박스는 회색이 될 뿐 **켜진 채로 남습니다.** 그 상태로 클론하면 화면은 "빈 영역을 건너뜁니다"라고 말하는데, 스냅샷이 없으면 엔진은 그 설정을 아예 버리고 통째 복사를 합니다. 결과가 잘못되지는 않지만 기대한 만큼 빨라지지 않고, 왜 그런지 알 방법이 없었습니다. 이제 스냅샷을 끄면 스마트 클론도 함께 꺼집니다.

### 같은 동작에는 같은 경고

- 완료 화면의 **`파티션 확장`** 버튼은 `마지막 파티션에 합치기`와 같은 일을 하는데, 라디오 쪽에만 있던 **"맨 뒤가 복구 파티션이면 그것이 커집니다"** 주의가 여기엔 없었습니다. 같은 동작이면 같은 경고를 해야 합니다.

### 내부 정리

- 엔진 어디에서도 읽지 않는 죽은 설정(`ZeroFreeSpace`)을 제거했습니다. 동작하는 것처럼 문서화된 채 남아 있으면, 나중에 화면에 체크박스가 달려 아무 일도 안 하는 옵션이 됩니다.

### 함께 대조했고 문제 없던 것

나머지 모든 클론 설정은 엔진이 실제로 읽습니다. `클론 시작` 버튼의 활성 조건에 영향을 주는 값은 빠짐없이 버튼에 반영되고, 취소는 일시정지를 먼저 풀어 주므로 멈춘 상태에서도 즉시 먹힙니다.

테스트 **174개** 통과.

---

**설치**: `DiskMigrator-v0.6.2-win-x64.exe`를 받아 관리자 권한으로 실행하십시오. .NET 설치가 필요 없는 단일 실행 파일입니다.


---

## v0.6.1 — 화면 정렬 마감 + 남는 공간 배선 정합성

*2026-07-20 · 태그 `v0.6.1`*

화면을 계획대로 다듬고, 남는 공간 설정이 조용히 다른 일을 하던 문제들을 잡았습니다.

### 한눈에 알아보기

- **드라이브 문자를 앞에 고정** — 목록에서 `C:` `E:` `D:`가 항상 줄 맨 앞 같은 자리에 옵니다. 예전에는 요약 문자열에 섞여 있어, 앞에 EFI·MSR이 있는 시스템 디스크는 문자가 세 번째로 밀렸습니다.
- **막대 위에도 드라이브 문자** — 범례를 눈으로 따라가지 않아도 어느 조각이 `C:`인지 바로 보입니다.
- **범례 줄맞춤** — 항목 글자 길이에 따라 한 줄에 2개도 3개도 되던 것을 2칸 격자로 고정하고, 이름 칸 폭을 묶어 세로선을 맞췄습니다.
- **목록 정렬** — 두 목록의 위·아래끝이 어긋나던 문제를 고쳤습니다. 항목 사이 구분선과 고른 항목의 왼쪽 강조 띠가 생겼습니다.

### 조용히 다른 일을 하던 것들

- **입력 크기가 화면과 다른 기준이었습니다.** 화면의 모든 크기는 1024 기준인데 `새 총 크기(GB)`만 1000 기준으로 읽었습니다. 범례의 `930.49 GB`를 그대로 입력하면 **약 7% 작은** 파티션이 됐습니다.
- **`새 총 크기` 라디오가 화면에만 있었습니다.** 선택 상태를 프로그램이 들고 있지 않아, 어떤 계기로 되돌아가면 입력한 크기 대신 **남는 공간 전부**를 쓰게 됩니다 — 화면은 그렇게 보이지 않습니다.
- **미리보기와 실제 실행이 서로 다른 판정을 했습니다.** 크기 입력이 잘못됐을 때 미리보기는 "남는 공간을 전부 채운" 막대를 그려 놓고, 시작을 누르면 거부했습니다.

### 이유를 제때, 보이는 곳에서

- 넓힐 수 없는 요청(예: 930 GB 파티션에 `600`)이면 `변경 후` 막대가 사라지던 자리에 **이유를 적습니다**. 예전에는 그림만 소리 없이 없어지고, 이유는 `클론 시작`을 눌러야 나왔습니다.
- 시작할 수 없는 상태면 **버튼이 꺼지고 사유가 뜹니다** — 몇 시간짜리 작업을 시작한 뒤에 알리지 않습니다.
- 크기를 **치는 대로** 막대와 버튼이 갱신됩니다.
- 오류 문구의 크기 표기도 화면과 같은 기준으로 통일했습니다.

### 내부

남는 공간 판정을 `FreeSpacePlanner`(순수 함수)로 모아, 미리보기·시작 버튼·실제 실행이 같은 하나를 부릅니다. 셋이 갈라질 수 없습니다. 테스트 **174개** 통과(v0.6.0 대비 18개 추가).

---

**설치**: `DiskMigrator-v0.6.1-win-x64.exe`를 받아 관리자 권한으로 실행하십시오. .NET 설치가 필요 없는 단일 실행 파일입니다.


---

## DiskMigrator v0.6.0 — 화면 정련과 숨어 있던 버그 수정

*2026-07-20 · 태그 `v0.6.0`*

Windows 디스크 전체 클론 도구. 실행 중인 시스템 디스크를 포함해 섹터 단위로 복제하고, 클론이 다른 하드웨어에서도 부팅되도록 마이그레이션합니다.

### v0.6.0 — 화면 정련과, 그 과정에서 찾은 버그들

기능은 그대로 두고 **화면을 다시 짰습니다.** 그런데 화면을 손보다 **기존 코드에 숨어 있던 버그 3건**을 찾았습니다. 그쪽이 더 중요합니다.

---

### 🔴 고친 버그

#### 원본과 대상이 저절로 같은 디스크로 선택되던 문제

원본 목록에서 디스크를 고르면 **대상 목록의 선택까지 같이 움직였습니다.** 두 목록이 같은 디스크 목록을 공유하는데, WPF가 그 목록의 "현재 항목"을 두 목록에서 공유하기 때문이었습니다.

디스크를 통째로 지우는 도구에서 원본과 대상이 저절로 같아지는 것은 위험합니다. 안전 검사가 "같은 디스크"로 막아주긴 했지만, **고르지도 않은 선택이 일어나는 것 자체가 결함**입니다.

이 버그는 v0.5.0에도 있었습니다. 배치 막대를 넣고 나서야 "양쪽 그림이 똑같다"로 눈에 띄었습니다.

#### 옵션 두 개를 켜면 하나가 조용히 무시되던 문제

`마지막 파티션 확장`과 `파티션 리사이즈`는 사실 같은 질문의 답입니다 — *"남는 공간을 누구에게 줄까?"*. 그런데 별개 체크박스라 **둘 다 켤 수 있었고**, 그러면 리사이즈가 이기고 다른 하나는 조용히 버려졌습니다. 화면에는 아무 표시도 없었습니다.

이제 **하나의 선택지**로 합쳤습니다. 셋 중 하나만 고를 수 있으므로 그런 조합 자체가 존재하지 않습니다.

#### 같은 디스크가 두 가지 크기로 표시되던 문제

배치 막대에는 `3.64 TB`, 삭제 경고 문구에는 `4000.8 GB`. 16MB짜리 파티션은 `0.0 GB`로 뭉개졌습니다. 크기 표기를 한 곳으로 통일했습니다.

---

### 화면이 바뀐 곳

#### 복제 후 모습을 미리 보여줍니다

대상 막대는 **"지금 지워질 것"** 을 보여줍니다. 그런데 그 아래에서 "남는 공간을 어떻게 할까요"를 물으니 앞뒤가 맞지 않았습니다 — 남는 공간은 복제가 끝난 뒤에야 생기니까요.

이제 **`변경 후`** 막대를 따로 그립니다. 선택을 바꾸면 즉시 다시 그려져, **결과를 보면서** 고를 수 있습니다.

세 막대(원본 · 대상 · 변경 후)는 높이·색·사용량 띠까지 **똑같은 모양**이고 라벨로만 시점을 가릅니다. 한쪽만 다르게 그리면 다른 의미의 그래프로 착각하기 때문입니다.

#### 남는 공간 선택이 대상 막대 바로 아래로

디스크를 보는 자리와 결정하는 자리를 같게 했습니다. 제목에 실제 용량이 들어가고(`남는 공간 2.73 TB 를 어떻게 할까요`), 각 선택지에 제약과 함정을 함께 적었습니다:

- **그대로 둡니다** — 미할당으로 남겨 두고 나중에 디스크 관리에서
- **마지막 파티션에 합치기** — 간단하고 안전 · MBR도 가능. *다만 맨 뒤가 복구 파티션이면 그것이 커집니다*
- **파티션 조정** — C: 처럼 중간 파티션도 · GPT 원본만

#### 막대마다 어느 디스크인지 표시

막대만 떼어놓고 보면 어느 쪽인지 알 수 없었습니다. 열 제목은 목록 맨 위에 있어 막대를 들여다보는 동안 시야에서 사라집니다. 이제 막대 옆에서 한 번 더 말해 줍니다 — **`[원본] … 읽기만 합니다`** / **`[대상] … 이 디스크가 지워집니다`**.

#### 확인 입력과 시작 버튼을 한 덩어리로

지우기 직전 가장 중요한 컨트롤이 화면에서 가장 눈에 안 띄었습니다. 입력란을 키워 버튼 옆에 붙이고, **버튼이 회색일 때 이유를 적습니다** — "확인 입력이 아직 완료되지 않아 시작할 수 없습니다", "대상 디스크를 고르십시오" 처럼.

#### 색과 글자

채도를 낮추고 회색을 따뜻한 쪽으로 옮겼습니다. 다만 **위험색(빨강)은 그대로 뒀습니다** — 디스크를 통째로 지우는 경고는 망설임 없이 빨강이어야 하고, 주변이 차분해질수록 오히려 더 또렷하게 보입니다.

배지도 성격으로 갈랐습니다: `시스템`(지우면 치명적)은 빨강, `착탈식`(단순 사실)은 파랑.

글자 크기는 이름 있는 6단계로 통일했습니다. 예전에는 10·11·12·13이 3픽셀 안에 몰려 있어 크기 차이가 위계로 읽히지 않았고, 섹션 제목이 디스크 이름보다 작은 역전도 있었습니다.

#### 목록이 사라지지 않습니다

내용이 늘어나면 디스크 목록이 먼저 줄어들어, 창이 좁을 때 아예 보이지 않는 경우가 있었습니다. 목록에 최소 높이를 보장하고, 넘치는 만큼만 스크롤하도록 바꿨습니다.

---

### 그대로 유지되는 것

작은 대상으로 맞춤 클론(v0.4.0), 파티션 확대 리사이즈(v0.3.0), 실행 중인 시스템 디스크 클론(VSS), 하드웨어 독립 마이그레이션(Universal Restore), 부팅 구성 검사·부팅 복구(0xc000000e), 스마트 클론, 복제-중-해시 검증, GPT 백업 헤더 보정.

### ⚠️ 경고

대상 디스크의 **모든 데이터가 영구 삭제**됩니다. 사용 전 반드시 백업하고 대상 디스크를 두 번 확인하십시오. 보증 없이 "있는 그대로" 제공됩니다.

### 알려진 동작 (Windows 제약)

관리자 권한으로 실행되므로, 이 창이 포커스를 가진 상태에서는 PrintScreen의 **범위 선택 캡처가 뜨지 않고 전체 화면 캡처로 대체**됩니다(작업 관리자 등 모든 관리자 앱이 동일). 범위 선택으로 찍으려면 창은 그대로 둔 채 다른 곳을 한 번 클릭해 포커스만 옮긴 뒤 PrintScreen을 누르십시오.

### 설치

`DiskMigrator-v0.6.0-win-x64.exe`를 내려받아 실행하십시오. .NET 런타임이 없는 PC에서도 동작하는 자체 포함 단일 실행 파일입니다(관리자 권한 필요).

총 156개 단위 테스트 통과. 화면은 실기에서 단계마다 눈으로 확인했습니다.

**전체 변경 사항**: https://github.com/withoz/DiskMigrator/compare/v0.5.0...v0.6.0


---

## DiskMigrator v0.5.0 — 파티션 배치 시각화

*2026-07-20 · 태그 `v0.5.0`*

Windows 디스크 전체 클론 도구. 실행 중인 시스템 디스크를 포함해 섹터 단위로 복제하고, 클론이 다른 하드웨어에서도 부팅되도록 마이그레이션합니다.

### v0.5.0 새 기능 — 파티션 배치 시각화

#### 디스크가 어떻게 나뉘어 있고 어디가 비었는지 한눈에

지금까지 디스크 목록은 총 용량과 파티션 요약 한 줄만 보여줬습니다. 볼륨 사용량은 내부적으로 수집하면서도 화면에 쓰지 않았고, 그래서 **"이 디스크가 더 작은 SSD에 들어가는가"** 같은 판단을 눈으로 할 수 없었습니다.

이제 원본·대상을 고르면 각 목록 아래에 **파티션 배치 막대**가 나타납니다.

- 파티션마다 **역할별 색** — EFI 시스템 · MSR(예약) · 데이터 · 복구 · 미할당
- 각 조각 안에 **볼륨 사용량을 띠로 겹쳐** 표시 (사용 1.53 TB / 여유 1.2 TB 같은 식)
- 막대 아래 **범례**에 파티션별 이름·크기·사용량
- **툴팁**에 파티션 번호·레이블·종류·크기·위치·사용/여유
- 막대 아래에 **"파티션이 차지한 끝"** 과 디스크 크기를 함께 표시 — 더 작은 디스크로 옮길 수 있는지 바로 판단할 수 있습니다
- 마지막 파티션 뒤에 빈 공간이 있으면 **"더 작은 디스크로 옮길 때 이만큼은 복제되지 않습니다"** 안내

아주 작은 파티션(1TB 디스크의 100MB EFI 파티션은 0.01%)은 그대로 그리면 한 픽셀도 안 되므로 최소 폭을 주어 그립니다. 비율이 왜곡되는 대신 **실제 크기는 항상 글자로** 함께 보여줍니다.

#### 왜 이게 중요한가

v0.4.0에서 "대상이 원본보다 작아도 파티션이 들어가면 복제한다"를 지원했는데, 들어가는지 아닌지는 **사용한 데이터 양이 아니라 파티션이 차지한 끝**의 문제입니다. 1TB 디스크에 데이터가 200GB뿐이어도 C: 파티션이 1TB를 차지하고 있으면 500GB SSD에 들어가지 않습니다. 이 구분이 화면에 드러나지 않으면 사용자는 왜 막히는지 알 수 없습니다. 배치 막대는 그 구분을 그대로 보여줍니다.

### 그대로 유지되는 것

작은 대상으로 맞춤 클론(v0.4.0), 파티션 확대 리사이즈(v0.3.0), 실행 중인 시스템 디스크 클론(VSS), 하드웨어 독립 마이그레이션(Universal Restore), 부팅 구성 검사·부팅 복구(0xc000000e), 스마트 클론(빈 영역 건너뛰기), 마지막 파티션 확장, 복제-중-해시 검증, GPT 백업 헤더 보정.

### ⚠️ 경고

대상 디스크의 **모든 데이터가 영구 삭제**됩니다. 사용 전 반드시 백업하고 대상 디스크를 두 번 확인하십시오. 보증 없이 "있는 그대로" 제공됩니다.

### 알려진 동작 (Windows 제약)

이 프로그램은 원시 디스크 접근 때문에 **관리자 권한**으로 실행됩니다. 그래서 이 창이 포커스를 가진 상태에서는 PrintScreen의 **범위 선택 캡처가 뜨지 않고 전체 화면 캡처로 대체**됩니다(Windows의 UIPI가 일반 권한 오버레이를 관리자 창 위에 띄우지 못하게 막습니다 — 작업 관리자 등 모든 관리자 앱이 동일). 범위 선택으로 찍으려면 **창은 그대로 둔 채 다른 곳을 한 번 클릭해 포커스만 옮긴 뒤** PrintScreen을 누르십시오.

### 설치

`DiskMigrator-v0.5.0-win-x64.exe`를 내려받아 실행하십시오. .NET 런타임이 없는 PC에서도 동작하는 자체 포함 단일 실행 파일입니다(관리자 권한 필요).

총 146개 단위 테스트 통과.

**전체 변경 사항**: https://github.com/withoz/DiskMigrator/compare/v0.4.0...v0.5.0


---

## DiskMigrator v0.4.0 — 작은 대상으로 맞춤 클론

*2026-07-20 · 태그 `v0.4.0`*

Windows 디스크 전체 클론 도구. 실행 중인 시스템 디스크를 포함해 섹터 단위로 복제하고, 클론이 다른 하드웨어에서도 부팅되도록 마이그레이션합니다.

### v0.4.0 새 기능 — 작은 대상으로 맞춤 클론

#### 대상이 원본보다 작아도 파티션이 들어가면 클론합니다

지금까지는 **디스크 전체 크기**만 비교해서, 대상이 원본보다 작으면 무조건 막았습니다. 그런데 뒤쪽이 비어 있는 디스크는 흔합니다 — 1TB 디스크에 파티션이 앞쪽 200GB만 있다면 500GB SSD에 **완벽히 들어가는데도** 클론할 수 없었습니다.

v0.4.0은 판정 기준을 **파티션이 실제로 차지한 끝**으로 바꿉니다. 파티션이 대상 안에 들어가면 그대로 복제합니다.

- 파티션은 **위치를 하나도 바꾸지 않고** 복사합니다. 오프셋·크기·고유 GUID가 전부 원본과 동일하므로 부트 구성(BCD가 GUID로 참조)이 그대로 유효합니다.
- 원본 끝에 있던 GPT 백업 헤더는 복사하지 않고, **줄어든 대상의 끝에 새로 씁니다.**
- 버려지는 것은 **마지막 파티션 뒤의 빈 공간**뿐입니다.
- **원본에는 아무것도 쓰지 않습니다.** 파일시스템을 줄이지 않으므로 원본은 그대로입니다.

대상이 작을 때는 버려지는 빈 공간 크기를 알리고 **사용자 확인**을 받습니다.

> **GPT 원본 전용입니다.** 백업 헤더 재작성에 기대는 방식이라 MBR은 아직 지원하지 않습니다.

#### 아직 지원하지 않는 것 — 파티션 자체를 줄이는 축소

파티션 **자체**가 대상보다 큰 경우는 NTFS 클러스터 재배치가 필요해 여전히 차단됩니다. 이때는 **원본에서 먼저 볼륨을 축소한 뒤** 클론하십시오 — 축소하고 나면 이번 맞춤 클론이 나머지를 자동으로 처리합니다. 차단 메시지에 필요한 최소 크기를 함께 안내합니다.

#### 실기 검증

원본 2GB(파티션은 앞쪽 616MB만) → 대상 1GB VHD로 end-to-end 검증했습니다: 파티션 3개(MSR 포함)의 오프셋·크기·고유 GUID가 전부 원본과 동일, 백업 헤더가 줄어든 끝(LBA 2097151)에 정확히 재생성, 대상 Online/Healthy/GPT, 파일 해시 8개 일치, chkdsk 클린, 그리고 **원본의 레이아웃·디스크 GUID·파일이 전부 무변경**임을 확인했습니다. 하니스는 `tools/vhd-fit-test.ps1`입니다.

### 그대로 유지되는 것

파티션 확대 리사이즈(GPT), 실행 중인 시스템 디스크 클론(VSS), 하드웨어 독립 마이그레이션(Universal Restore), 부팅 구성 검사·부팅 복구(0xc000000e), 스마트 클론(빈 영역 건너뛰기), 마지막 파티션 확장, 복제-중-해시 검증, GPT 백업 헤더 보정.

v0.3.1~v0.3.2의 리사이즈 안전장치(MBR 차단, GPT 재작성 실패를 손상으로 표시, 섹터 크기 가드, 크기 입력 검증)도 모두 포함됩니다.

### ⚠️ 경고

대상 디스크의 **모든 데이터가 영구 삭제**됩니다. 사용 전 반드시 백업하고 대상 디스크를 두 번 확인하십시오. 보증 없이 "있는 그대로" 제공됩니다.

### 설치

`DiskMigrator-v0.4.0-win-x64.exe`를 내려받아 실행하십시오. .NET 런타임이 없는 PC에서도 동작하는 자체 포함 단일 실행 파일입니다(관리자 권한 필요).

총 135개 단위 테스트 통과. 맞춤 클론은 실물 VHD로 end-to-end 검증했습니다.

**전체 변경 사항**: https://github.com/withoz/DiskMigrator/compare/v0.3.2...v0.4.0


---

## DiskMigrator v0.3.2 — 리사이즈 안전장치·입력 검증

*2026-07-20 · 태그 `v0.3.2`*

Windows 디스크 전체 클론 도구. 실행 중인 시스템 디스크를 포함해 섹터 단위로 복제하고, 클론이 다른 하드웨어에서도 부팅되도록 마이그레이션합니다.

### v0.3.2 — 리사이즈 안전장치와 입력 검증

v0.3.1에 이어 파티션 확대 리사이즈의 남은 위험 지점을 정리했습니다. 기능 추가는 없고, **잘못된 조건에서 조용히 엉뚱한 결과가 나오던 경로를 모두 막는** 변경입니다.

#### 크기 입력이 잘못되면 조용히 "남는 공간 전부"가 되던 문제

'지정 크기' 모드에서 입력값이 숫자로 읽히지 않으면 내부적으로 "크기 미지정"으로 처리되어, **남는 공간을 전부 몰아주는 요청**이 됐습니다. 사용자가 "500GB로 늘리기"를 의도했는데 오타 하나로 파티션이 디스크 끝까지 커지는데도 아무 경고가 없었습니다.

이제 입력이 올바르지 않으면 **작업을 시작하기 전에 멈추고** 이유를 알립니다. 대상 디스크에는 아무것도 쓰지 않습니다. 입력은 사용자의 지역 설정과 마침표 표기를 모두 받아들입니다(`1.5`, `1,5`).

#### 논리 섹터 크기가 다른 원본↔대상 리사이즈 차단

GPT는 파티션 위치를 LBA(섹터 번호)로 저장합니다. 리사이즈는 원본 GPT를 대상에 복사한 뒤 그 LBA만 새 배치로 고치는데, 원본과 대상의 논리 섹터 크기가 다르면(512B ↔ 4096B) 같은 숫자가 서로 다른 단위가 되어 재배치가 어긋납니다.

이제 이 조합에서는 **클론을 시작하기 전에 중단**하고, GUI에서도 리사이즈 옵션이 아예 켜지지 않습니다.

#### '지정 크기(GB)' → '새 총 크기(GB)'

늘린 뒤의 전체 크기인지 추가할 크기인지 모호했습니다. 라벨을 바꾸고 안내에 "추가할 크기가 아닙니다"를 명시했습니다.

### v0.3.1에서 들어간 것 (요약)

- **MBR 원본 리사이즈 차단** — 리사이즈는 GPT 전용인데 MBR을 막지 않아, 부팅 불가 클론이 "완료"로 표시되던 실기 버그
- **리사이즈 GPT 재작성 실패를 손상으로 표시** — 파티션 테이블이 옛 위치를 가리키는 깨진 클론을 "정확한 사본"이라는 성공으로 보여주던 문제

### 그대로 유지되는 것

파티션 확대 리사이즈(GPT), 실행 중인 시스템 디스크 클론(VSS), 하드웨어 독립 마이그레이션(Universal Restore), 부팅 구성 검사·부팅 복구(0xc000000e), 스마트 클론(빈 영역 건너뛰기), 마지막 파티션 확장, 복제-중-해시 검증, GPT 백업 헤더 보정.

### ⚠️ 경고

대상 디스크의 **모든 데이터가 영구 삭제**됩니다. 사용 전 반드시 백업하고 대상 디스크를 두 번 확인하십시오. 보증 없이 "있는 그대로" 제공됩니다.

### 설치

`DiskMigrator-v0.3.2-win-x64.exe`를 내려받아 실행하십시오. .NET 런타임이 없는 PC에서도 동작하는 자체 포함 단일 실행 파일입니다(관리자 권한 필요).

총 124개 단위 테스트 통과. 리사이즈 엔진은 실물 VHD로 end-to-end 검증했습니다.

**전체 변경 사항**: https://github.com/withoz/DiskMigrator/compare/v0.3.1...v0.3.2


---

## DiskMigrator v0.3.1 — 파티션 리사이즈 안전성 수정

*2026-07-20 · 태그 `v0.3.1`*

Windows 디스크 전체 클론 도구. 실행 중인 시스템 디스크를 포함해 섹터 단위로 복제하고, 클론이 다른 하드웨어에서도 부팅되도록 마이그레이션합니다.

### v0.3.1 — 파티션 리사이즈 안전성 수정 (권장 업데이트)

v0.3.0의 파티션 확대 리사이즈에서 **부팅 불가 클론이 "완료"로 표시되던 문제 2건**을 고쳤습니다. 리사이즈 기능을 쓰신다면 이 버전으로 올리십시오.

#### 🔴 MBR 원본 리사이즈 차단

파티션 리사이즈는 GPT 전용인데 MBR 원본을 막지 않았습니다. MBR 디스크에 확대 리사이즈를 걸면 파티션 데이터는 새 위치로 복사되지만 파티션 테이블은 옛 위치를 가리켜, **배치가 깨진 부팅 불가 클론이 "완료"로 표시**되었습니다(실기에서 확인).

이제 MBR 원본에 리사이즈를 요청하면 **클론을 시작하기 전에 즉시 중단**하고 이유를 알립니다. GUI에서도 GPT 원본일 때만 리사이즈 옵션이 켜집니다.

> MBR 디스크에서 마지막이 아닌 파티션을 넓히려면 `mbr2gpt`로 GPT 변환 후 리사이즈하십시오. MBR 리사이즈 자체 지원은 향후 과제입니다.

#### 🔴 리사이즈 GPT 재작성 실패를 손상으로 표시

리사이즈 클론은 파티션 데이터를 **새 오프셋**에 씁니다. 클론 후 파티션 테이블을 새 배치로 다시 쓰는 단계가 실패하면 테이블이 옛 위치를 가리켜 디스크를 쓸 수 없는데, 기존에는 이것이 경고 한 줄로만 묻히고 결과 화면은 **"클론 완료 — 원본의 정확한 사본"** 이라는 성공을 표시했습니다.

이제 이 경우 **"이 디스크로 부팅하지 마십시오"** 경고와 함께 실패로 표시하고, 깨진 디스크에 파티션 확장·하드웨어 독립화를 시도하지 않습니다.

### 그대로 유지되는 것

파티션 확대 리사이즈(GPT), 실행 중인 시스템 디스크 클론(VSS), 하드웨어 독립 마이그레이션(Universal Restore), 부팅 구성 검사·부팅 복구(0xc000000e), 스마트 클론(빈 영역 건너뛰기), 마지막 파티션 확장, 복제-중-해시 검증, GPT 백업 헤더 보정.

### ⚠️ 경고

대상 디스크의 **모든 데이터가 영구 삭제**됩니다. 사용 전 반드시 백업하고 대상 디스크를 두 번 확인하십시오. 보증 없이 "있는 그대로" 제공됩니다.

### 설치

`DiskMigrator-v0.3.1-win-x64.exe`를 내려받아 실행하십시오. .NET 런타임이 없는 PC에서도 동작하는 자체 포함 단일 실행 파일입니다(관리자 권한 필요).

총 124개 단위 테스트 통과. 리사이즈 엔진은 실물 VHD로 end-to-end 검증했습니다.

**전체 변경 사항**: https://github.com/withoz/DiskMigrator/compare/v0.3.0...v0.3.1


---

## DiskMigrator v0.3.0 — 파티션 확대 리사이즈

*2026-07-19 · 태그 `v0.3.0`*

Windows 디스크 전체 클론 도구. 실행 중인 시스템 디스크를 포함해 섹터 단위로 복제하고, 클론이 다른 하드웨어에서도 부팅되도록 마이그레이션합니다.

### v0.3.0 새 기능 — 파티션 확대 리사이즈

#### 📐 고른 파티션을 넓히고 뒤 파티션은 자동으로 밀어냄
"마지막 파티션 확장"(v0.2.0)은 *마지막* 파티션만 넓힙니다. 하지만 전형적인 Windows 디스크는 `[ESP][MSR][C:][복구]`처럼 C: 뒤에 복구 파티션이 있어, C:를 넓히려면 복구 파티션을 뒤로 밀어야 합니다. v0.3.0은 **고른 파티션을 확대하고 그 뒤 파티션들을 오른쪽으로 밀어** 이 문제를 해결합니다.

- 각 파티션 데이터는 1:1 블록 복사(스마트 클론과 호환)이되 **새 오프셋**으로 옮겨집니다.
- 클론 후 GPT를 새 배치로 다시 쓰면서 **파티션의 고유 GUID·타입·이름을 보존**합니다(BCD가 파티션을 GUID로 참조하므로 보존하지 않으면 부팅이 깨집니다).
- 확대한 파티션의 NTFS는 `diskpart extend`로 늘립니다. 클론 중 자동 확장은 원본 동시 연결로 실패하기 쉬우므로, **대상을 단독 연결한 뒤** GUI "파티션 확장" 버튼 또는 CLI `--expand-part <번호>`로 마무리합니다.

#### 사용법
- **GUI** — 대상이 원본보다 크면 "파티션 리사이즈 (확대)" 옵션이 활성화됩니다. 확대할 NTFS 파티션과 크기(남는 공간 전부 / 지정 GB)를 고르면 됩니다.
- **CLI** — `--grow <파티션번호> [--grow-gb <GB>]`로 클론, 완료 후 `--expand-part <번호>`로 마무리.

> **확대 전용입니다.** 대상은 원본보다 커야 하며, 축소(대상 < 원본)는 파일시스템 인식 NTFS 축소가 필요해 다음 버전 계획입니다.

**실기 검증**: `[GROWME 300MB][TAIL 200MB]` 원본을 2GB 대상으로 클론하며 GROWME 확대 → TAIL 자동 시프트, 고유 GUID 보존, GROWME 300MB→1.79GB(볼륨 실사용 증가), 파일 해시 전부 일치, chkdsk 클린.

### 그대로 유지되는 것 (v0.1~0.2)
실행 중인 시스템 디스크 클론(VSS), 하드웨어 독립 마이그레이션(Universal Restore), 부팅 구성 검사·부팅 복구(0xc000000e), 스마트 클론(빈 영역 건너뛰기), 마지막 파티션 확장, 복제-중-해시 검증, GPT 백업 헤더 보정.

### ⚠️ 경고
대상 디스크의 **모든 데이터가 영구 삭제**됩니다. 사용 전 반드시 백업하고 대상 디스크를 두 번 확인하십시오. 보증 없이 "있는 그대로" 제공됩니다.

### 설치
`DiskMigrator-v0.3.0-win-x64.exe`를 내려받아 실행하십시오. .NET 런타임이 없는 PC에서도 동작하는 자체 포함 단일 실행 파일입니다(관리자 권한 필요).

총 124개 단위 테스트 통과. 파티션 리사이즈는 엔진·CLI를 실물 VHD로 end-to-end 검증했습니다(GUI는 클릭-스루 미검증).


---

## DiskMigrator v0.2.0 — 스마트 클론 + 마지막 파티션 확장

*2026-07-19 · 태그 `v0.2.0`*

Windows 디스크 전체 클론 도구. 실행 중인 시스템 디스크를 포함해 섹터 단위로 복제하고, 클론이 다른 하드웨어에서도 부팅되도록 마이그레이션합니다.

### v0.2.0 새 기능 — 클론 엔진 확장

#### 🧠 스마트 클론 (빈 영역 건너뛰기)
NTFS 할당 비트맵(`FSCTL_GET_VOLUME_BITMAP`)을 읽어 **실제로 쓰이는 클러스터만** 복사합니다. 1TB 디스크에 실데이터가 200GB뿐이면 200GB만 복사하므로 크게 빨라집니다.
- 비트맵은 VSS **스냅샷에서** 읽어 드리프트에 안전(스냅샷 모드 전용).
- NTFS가 아니거나 비트맵을 못 읽으면 해당 파티션은 **통째 복사로 자동 폴백** — 정확한 복제가 언제나 우선.
- 실기 검증: 라이브 C:(931GB 중 392GB 사용) → 42%만 복사, 539GB 건너뜀. 작은 VHD→큰 VHD end-to-end에서 chkdsk 클린 + SHA256 전부 일치.

#### 📐 마지막 파티션 확장
작은 디스크를 큰 디스크로 클론한 뒤 남는 미할당 공간을 마지막 파티션에 합칩니다. `diskpart extend`로 파티션과 NTFS를 **한 번에 정합적으로** 확장(중간에 깨진 상태 없음).
- 클론 중 자동 확장은 원본 동시 연결로 실패하기 쉬우므로, **대상을 단독 연결한 뒤** GUI "파티션 확장" 버튼 또는 CLI `--expand-last`로 마무리하는 것이 정석.
- 실기 검증: 단독 대상 1GB → 1.98GB 확장, 파일 유지, 미할당 0.

### 그대로 유지되는 것 (v0.1.4)
실행 중인 시스템 디스크 클론(VSS), 하드웨어 독립 마이그레이션(Universal Restore), 부팅 구성 검사·부팅 복구(0xc000000e), 복제-중-해시 검증, GPT 백업 헤더 보정.

### ⚠️ 경고
대상 디스크의 **모든 데이터가 영구 삭제**됩니다. 사용 전 반드시 백업하고 대상 디스크를 두 번 확인하십시오. 보증 없이 "있는 그대로" 제공됩니다.

**대상 용량은 원본 이상이어야 합니다.** 스마트 클론은 복사량만 줄일 뿐 파티션 위치는 원본과 같게 두므로, 대상이 원본보다 작으면 복제할 수 없습니다(파일시스템 인식 축소는 다음 버전 계획).

### 설치
`DiskMigrator-v0.2.0-win-x64.exe`를 내려받아 실행하십시오. .NET 런타임이 없는 PC에서도 동작하는 자체 포함 단일 실행 파일입니다(관리자 권한 필요).

총 105개 단위 테스트 통과.


---

## DiskMigrator v0.1.4 - 하드웨어 독립 부팅 실증 + 부팅 복구

*2026-07-18 · 태그 `v0.1.4`*

Windows 디스크 전체 클론 도구. 실행 중인 시스템 디스크를 섹터 단위로 복제하고, 클론이 **다른 하드웨어에서도 부팅**되도록 마이그레이션합니다.

**하드웨어 독립 부팅 실증** — 클론한 시스템 디스크를 원본과 다른 PC(ASRock B360M / i7-9700)에 옮겨 부팅에 성공했습니다.

#### 주요 기능
- **실행 중인 시스템 디스크 클론** (VSS 스냅샷) + 복제-중-해시 검증
- **Universal Restore** — 표준 저장소 드라이버를 부팅 시작으로 설정해 다른 PC에서 `0x0000007B` 예방
- **부팅 구성 검사** (`--boot-check` / GUI) — 부트로더·BCD·winload·드라이버·장치 참조를 옮기기 전에 점검
- **부팅 복구** (`--fix-boot` / GUI) — 디스크 서명 충돌로 생기는 `0xc000000e`를 자동 수정
- 대상이 더 클 때 GPT 백업 헤더 자동 보정, 불량 섹터 처리, 진행률·일시정지·취소

#### 다른 PC로 옮길 때
- 대상 BIOS가 Intel RST(RAID) 모드이면 `0x0000007B` → **AHCI로 변경**하세요.
- 클론을 원본과 동시에 켜두지 마세요(`0xc000000e` 유발). 발생 시 **부팅 복구**로 해결.

#### 자산
- `DiskMigrator-v0.1.4-win-x64.exe` — 자체 포함 단일 exe (런타임 불필요, ~156MB, 관리자 권한 실행)

⚠️ 되돌릴 수 없는 작업입니다. 반드시 중요한 데이터를 먼저 백업하세요. 자세한 내용은 [README](https://github.com/withoz/DiskMigrator#readme) 참조.
